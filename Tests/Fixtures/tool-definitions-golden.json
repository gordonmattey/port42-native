[
  {
    "description" : "Speak text aloud using text-to-speech",
    "input_schema" : {
      "properties" : {
        "rate" : {
          "description" : "Speech rate 0.1-1.0 (default 0.5)",
          "type" : "number"
        },
        "text" : {
          "description" : "Text to speak",
          "type" : "string"
        }
      },
      "required" : [
        "text"
      ],
      "type" : "object"
    },
    "name" : "audio_speak"
  },
  {
    "description" : "Take a screenshot of an open browser session. Returns base64 PNG.",
    "input_schema" : {
      "properties" : {
        "sessionId" : {
          "description" : "Browser session ID from browser_open",
          "type" : "string"
        }
      },
      "required" : [
        "sessionId"
      ],
      "type" : "object"
    },
    "name" : "browser_capture"
  },
  {
    "description" : "Close a browser session",
    "input_schema" : {
      "properties" : {
        "sessionId" : {
          "description" : "Browser session ID to close",
          "type" : "string"
        }
      },
      "required" : [
        "sessionId"
      ],
      "type" : "object"
    },
    "name" : "browser_close"
  },
  {
    "description" : "Open a URL in a headless browser and return the page title. Use browser_text to read page content after opening.",
    "input_schema" : {
      "properties" : {
        "url" : {
          "description" : "The URL to open (http or https)",
          "type" : "string"
        }
      },
      "required" : [
        "url"
      ],
      "type" : "object"
    },
    "name" : "browser_open"
  },
  {
    "description" : "Extract text content from an open browser session",
    "input_schema" : {
      "properties" : {
        "selector" : {
          "description" : "CSS selector to extract from (default: body)",
          "type" : "string"
        },
        "sessionId" : {
          "description" : "Browser session ID from browser_open",
          "type" : "string"
        }
      },
      "required" : [
        "sessionId"
      ],
      "type" : "object"
    },
    "name" : "browser_text"
  },
  {
    "description" : "Capture a photo from the device camera. Returns a base64 PNG image.",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "camera_capture"
  },
  {
    "description" : "Post to a port's chat. Every port has one: pass port 0 for the desktop, a space id, or a port's id. The entry is attributed to you, the caller, and every subscriber of the port gets a `chat` event carrying it.",
    "input_schema" : {
      "properties" : {
        "port" : {
          "description" : "Whose chat: `0` (the desktop), a space id, or a port id \/ udid \/ title.",
          "type" : "string"
        },
        "text" : {
          "description" : "What to say.",
          "type" : "string"
        }
      },
      "required" : [
        "port",
        "text"
      ],
      "type" : "object"
    },
    "name" : "chat_post"
  },
  {
    "description" : "Read a port's chat, oldest first. Pass `after` (a seq you have seen) to get only what is newer. Returns { entries, last }, where `last` is the newest seq in the chat (0 when empty).",
    "input_schema" : {
      "properties" : {
        "after" : {
          "description" : "Only entries with a seq greater than this.",
          "type" : "integer"
        },
        "limit" : {
          "description" : "At most this many, the newest ones (default 50, max 200).",
          "type" : "integer"
        },
        "port" : {
          "description" : "Whose chat: `0` (the desktop), a space id, or a port id \/ udid \/ title.",
          "type" : "string"
        }
      },
      "required" : [
        "port"
      ],
      "type" : "object"
    },
    "name" : "chat_read"
  },
  {
    "description" : "Read the current clipboard contents. Returns text or base64 image data.",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "clipboard_read"
  },
  {
    "description" : "Write text to the system clipboard",
    "input_schema" : {
      "properties" : {
        "data" : {
          "description" : "The text to copy to clipboard",
          "type" : "string"
        }
      },
      "required" : [
        "data"
      ],
      "type" : "object"
    },
    "name" : "clipboard_write"
  },
  {
    "description" : "Get details about a specific companion by ID",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The companion's ID",
          "type" : "string"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "companions_get"
  },
  {
    "description" : "List the companions in a space with their names, models, and trigger modes. Defaults to YOUR space — the companions you share this space with — because a companion acts within its space, not the whole instance. Pass space_id to target a different space, or space_id:\"*\" for the full global roster across every space in the Port42 instance (rarely what you want).",
    "input_schema" : {
      "properties" : {
        "space_id" : {
          "description" : "Omit for your own space (the default). A space id targets that space. \"*\" returns the whole-instance roster.",
          "type" : "string"
        }
      },
      "type" : "object"
    },
    "name" : "companions_list"
  },
  {
    "description" : "List the contents of a directory in the Port42 data directory. Relative paths only (e.g. \"scopes\/strategy\" or \"scopes\/strategy\/decisions\"). Returns a sorted list of filenames.",
    "input_schema" : {
      "properties" : {
        "path" : {
          "description" : "Relative path to the directory (e.g. \"scopes\/strategy\")",
          "type" : "string"
        }
      },
      "required" : [
        "path"
      ],
      "type" : "object"
    },
    "name" : "file_list"
  },
  {
    "description" : "Create a directory (and any missing parent directories) in the Port42 data directory. Relative paths only (e.g. \"scopes\/strategy\/decisions\").",
    "input_schema" : {
      "properties" : {
        "path" : {
          "description" : "Relative path to create (e.g. \"scopes\/strategy\/decisions\")",
          "type" : "string"
        }
      },
      "required" : [
        "path"
      ],
      "type" : "object"
    },
    "name" : "file_mkdir"
  },
  {
    "description" : "Read a file. Use a relative path (e.g. \"scopes\/strategy\/scope.md\") to read from the Port42 data directory without a file picker. Use an absolute path for picker-approved files.",
    "input_schema" : {
      "properties" : {
        "encoding" : {
          "description" : "utf8 (default) or base64",
          "type" : "string"
        },
        "path" : {
          "description" : "Relative path within Port42 data directory (e.g. \"scopes\/strategy\/scope.md\") or absolute path for picker-approved files.",
          "type" : "string"
        }
      },
      "required" : [
        "path"
      ],
      "type" : "object"
    },
    "name" : "file_read"
  },
  {
    "description" : "Write a file. Use a relative path (e.g. \"scopes\/strategy\/facts.md\") to write to the Port42 data directory — parent directories are created automatically. Use an absolute path for picker-approved files.",
    "input_schema" : {
      "properties" : {
        "data" : {
          "description" : "Content to write",
          "type" : "string"
        },
        "encoding" : {
          "description" : "utf8 (default) or base64",
          "type" : "string"
        },
        "path" : {
          "description" : "Relative path within Port42 data directory (e.g. \"scopes\/strategy\/facts.md\") or absolute path for picker-approved files.",
          "type" : "string"
        }
      },
      "required" : [
        "path",
        "data"
      ],
      "type" : "object"
    },
    "name" : "file_write"
  },
  {
    "description" : "Return the Port42 API reference. Pass topic:\"ports\" for the port-authoring manual (read it BEFORE building or editing a port: sizing, module-scope, patterns, the design system). No topic returns the full method reference.",
    "input_schema" : {
      "properties" : {
        "topic" : {
          "description" : "Optional. \"ports\" = the port-authoring manual. Omit for the API reference.",
          "type" : "string"
        }
      },
      "type" : "object"
    },
    "name" : "help"
  },
  {
    "description" : "Send a macOS system notification",
    "input_schema" : {
      "properties" : {
        "body" : {
          "description" : "Notification body text",
          "type" : "string"
        },
        "title" : {
          "description" : "Notification title",
          "type" : "string"
        }
      },
      "required" : [
        "title",
        "body"
      ],
      "type" : "object"
    },
    "name" : "notify_send"
  },
  {
    "description" : "Read what a port has printed — a web port's console.log\/warn\/error, or a terminal's output. Returns the most recent lines, oldest first, each with a level and a timestamp. Use it to debug a port you built: a generative port that throws at runtime says so here, and a terminal whose command died says nothing else at all. Pass the id from ports_list; tail defaults to 100.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list), or a terminal's name.",
          "type" : "string"
        },
        "tail" : {
          "description" : "How many recent lines to return (default 100).",
          "type" : "integer"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_console"
  },
  {
    "description" : "Create a port and return its id. The uniform way to make any port. type:\"web\" needs html (a full port HTML body) and renders inline in chat. type:\"terminal\" needs command and opens a native terminal (runs in \/bin\/zsh; the command is typed in — claude\/gemini get the Port42 hooks). type:\"browser\" needs url and opens an embedded browser tile with an address bar that follows links. type:\"chat\" reveals that space's chat port (idempotent — one chat per space; brings it back if parked, popped out or closed, and a DM is a space, so pass its space_id to open that conversation). For terminals you may also pass args, cwd, systemPrompt (companion personality), env, and initialInput (a line left waiting, unsent, in the CLI's input box). Drive the result with port_push (input to terminals, data to web ports) and list with ports_list. Pass space_id to target a space (default: current).",
    "input_schema" : {
      "properties" : {
        "args" : {
          "description" : "type:\"terminal\" — arguments for the command.",
          "items" : {
            "type" : "string"
          },
          "type" : "array"
        },
        "command" : {
          "description" : "type:\"terminal\" — executable\/CLI to run (e.g. \"bash\", \"htop\", \"claude\").",
          "type" : "string"
        },
        "cwd" : {
          "description" : "type:\"terminal\" — working directory (default: home).",
          "type" : "string"
        },
        "env" : {
          "description" : "type:\"terminal\" — custom environment variables for the shell.",
          "type" : "object"
        },
        "html" : {
          "description" : "type:\"web\" — full port HTML body (include a <title> and <meta name=\"version\">).",
          "type" : "string"
        },
        "initialInput" : {
          "description" : "type:\"terminal\" — a line typed into the CLI once it is up but NOT submitted: it waits in the input box for the user to press Enter. For handing someone a first prompt to run. Use port_push instead to actually send input.",
          "type" : "string"
        },
        "presentation" : {
          "description" : "Where the port appears: \"tiled\" (default, a desktop tile) or \"parked\".",
          "type" : "string"
        },
        "space_id" : {
          "description" : "Space to create the port in (default: current space).",
          "type" : "string"
        },
        "systemPrompt" : {
          "description" : "type:\"terminal\" — companion personality\/role appended to the CLI's system prompt.",
          "type" : "string"
        },
        "title" : {
          "description" : "Port title (default: derived from html <title>, or the command).",
          "type" : "string"
        },
        "type" : {
          "description" : "The port type to create.",
          "enum" : [
            "web",
            "terminal",
            "browser",
            "chat"
          ],
          "type" : "string"
        },
        "url" : {
          "description" : "type:\"browser\" — the page to open (e.g. \"https:\/\/example.com\").",
          "type" : "string"
        }
      },
      "required" : [
        "type"
      ],
      "type" : "object"
    },
    "name" : "port_create"
  },
  {
    "description" : "Delete a CLOSED port for good: its record, versions and chat. Close it first (port.manage close); an open port is refused, so nothing live is ever deleted in one step.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The closed port's id.",
          "type" : "string"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_delete"
  },
  {
    "description" : "Execute JavaScript on a live port. Use this to call functions, push data, or update state on an existing port without replacing its HTML. The JS runs in the port's webview context with access to window, document, and any globals the port defines.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "js" : {
          "description" : "JavaScript code to execute in the port's context. Return a value to get it back in the response, as {value, token}. A bare expression yields its value (multi-line is fine). A multi-statement body needs an explicit return: `foo(); 42` is a syntax error, `foo(); return 42;` works.",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "js",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_exec"
  },
  {
    "description" : "Read a WEB or BROWSER port's LIVE DOM — what is on screen right now, including everything its JS has changed since load. Use this, not port_get_html, when you need current state: port_get_html returns the stored SOURCE, which does not reflect any port_exec or port_push that has run since. Returns {html, token}; pass that token as 'expect' on your next write and it will be refused rather than clobber someone if the port moved in between.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "selector" : {
          "description" : "Optional CSS selector to read just one subtree. Omit for the whole document.",
          "type" : "string"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_get_dom"
  },
  {
    "description" : "Read the HTML of a port. Omit 'version' to get the current HTML. Pass 'version' (from port_history) to read a specific historical snapshot.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "version" : {
          "description" : "Optional version number (from port_history). Omit for current HTML.",
          "type" : "integer"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_get_html"
  },
  {
    "description" : "List all saved versions of a port by its UDID. Returns version number, createdBy, and createdAt for each snapshot. Use port_get_html with a version number to read a specific snapshot, or port_restore to roll back.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_history"
  },
  {
    "description" : "Manage a port. Actions: focus (raise to the front of the desktop), close (archive it: it can be reopened with port.reopen), minimize\/dock (off the desktop but still running), restore\/undock (bring a docked port back onto the desktop). Check the status field from ports_list — 'tiled' | 'parked' | 'docked'.",
    "input_schema" : {
      "properties" : {
        "action" : {
          "description" : "One of: focus, close, minimize, dock, restore, undock",
          "type" : "string"
        },
        "id" : {
          "description" : "The port's UDID or title",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "action",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_manage"
  },
  {
    "description" : "Move a port's tile to specific desktop coordinates. Use screen_info to get display bounds first.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "space_id" : {
          "description" : "Which desktop to move it on. A port kept from another space is a tile on BOTH, with a position on each. Defaults to the current space when the port is on it, else the port's home space.",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        },
        "x" : {
          "description" : "Horizontal position in desktop points",
          "type" : "number"
        },
        "y" : {
          "description" : "Vertical position in desktop points",
          "type" : "number"
        }
      },
      "required" : [
        "id",
        "x",
        "y",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_move"
  },
  {
    "description" : "Make a targeted edit to a port's HTML — replace an exact string with new content. Much safer than port_update for small changes because only the specified text is replaced; everything else is preserved exactly. Use port_get_html first to read the current HTML, find the exact string to replace, then call port_patch. Errors if 'search' is not found in the current HTML, so the port is never silently mangled. Snapshots the result the same as port_update.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "replace" : {
          "description" : "The string to replace it with.",
          "type" : "string"
        },
        "search" : {
          "description" : "The exact string to find in the current HTML. Must match exactly — copy it from port_get_html output.",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "search",
        "replace",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_patch"
  },
  {
    "description" : "A port emits its OWN state or event on its own Notify topic, for consumers watching via port_subscribe. This is a port broadcasting AS itself — distinct from port_push, which is input sent INTO a port. Only meaningful from inside a port; the topic is the calling port's own id, so there is no target argument. Use this instead of having a consumer reach in with port_exec to read state: the port publishes, consumers subscribe.",
    "input_schema" : {
      "properties" : {
        "kind" : {
          "description" : "Event kind, e.g. 'state', 'progress', 'error'. Namespaced on the way out: you publish 'state', subscribers see 'port.state', so a port cannot emit a system event like 'driver' or 'browser.load'.",
          "type" : "string"
        },
        "payload" : {
          "description" : "Any JSON value (object\/array\/string\/number) delivered as the Notify envelope's payload."
        }
      },
      "required" : [
        "kind"
      ],
      "type" : "object"
    },
    "name" : "port_publish"
  },
  {
    "description" : "Send input to a port — one verb, dispatched by the port's type. A WEB port receives the data as a 'port42:data' CustomEvent with the payload in event.detail. A TERMINAL port receives the data as raw keystrokes typed into the shell: end with a newline (e.g. \"ls\\n\") to run the command, or omit it to leave the line waiting unsubmitted. Use the id from ports_list. Prefer this over port_exec for data transfer.",
    "input_schema" : {
      "properties" : {
        "data" : {
          "description" : "For web ports: any JSON value (object\/array\/string\/number) delivered as event.detail. For terminal ports: a string of raw keystrokes (include \\n to execute). Required — omitting it is refused with missing_arg rather than sent as nothing, and a terminal refuses an explicit null because there is no keystroke for null."
        },
        "id" : {
          "description" : "The port's UDID (from ports_list), or a terminal's name.",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "data",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_push"
  },
  {
    "description" : "Rename a port. Sets the port's display title (shown in the title bar). Works for tiled, parked, docked, and inline ports. Use the port's id from ports_list.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "title" : {
          "description" : "The new title for the port",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "title",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_rename"
  },
  {
    "description" : "Reopen a closed port with its id, content, position and chat. A terminal relaunches its command in its last working directory. Closed ports are listed by ports_list with include_closed.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The closed port's id.",
          "type" : "string"
        }
      },
      "required" : [
        "id"
      ],
      "type" : "object"
    },
    "name" : "port_reopen"
  },
  {
    "description" : "Restore a port to a specific earlier version. The port's live HTML is replaced with the snapshot and a new version entry is recorded. Use port_history to find available version numbers.",
    "input_schema" : {
      "properties" : {
        "id" : {
          "description" : "The port's UDID (from ports_list)",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        },
        "version" : {
          "description" : "The version number to restore to (from port_history)",
          "type" : "integer"
        }
      },
      "required" : [
        "id",
        "version",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_restore"
  },
  {
    "description" : "Update an existing port's HTML content. The port can be identified by its UDID or title. Works whether the port is windowed or minimized.",
    "input_schema" : {
      "properties" : {
        "html" : {
          "description" : "The new HTML content for the port (full HTML, not a diff)",
          "type" : "string"
        },
        "id" : {
          "description" : "The port's UDID or title to identify which port to update",
          "type" : "string"
        },
        "token" : {
          "description" : "REQUIRED. The port's `token`, as it was when you composed this write — from ports_list, port_create, or whatever your last write returned. Without it the write is refused with 'token_required'; if the port has changed since, with 'stale_write'. Both carry the current token, so retry once with that instead of clobbering whoever moved it.",
          "type" : "string"
        }
      },
      "required" : [
        "id",
        "html",
        "token"
      ],
      "type" : "object"
    },
    "name" : "port_update"
  },
  {
    "description" : "List active ports. Each port has an id (UDID), title, capabilities array, status, spaceId, createdBy (an id) with createdByName (who that is, for display), and cwd (if it has a terminal). Terminal ports also report surfaceBound. Use capabilities: [\"terminal\"] to filter to terminal ports; pass space_id to list only that space's ports. Use the id field with port_push for reliable routing (raw keystrokes to terminals, data to web ports). Always show the id and capabilities fields when presenting results — they are required for follow-up tool calls.",
    "input_schema" : {
      "properties" : {
        "capabilities" : {
          "description" : "Filter to ports that have all of these capabilities. Examples: \"terminal\", \"claude-code\", \"browser\". Omit to list all ports.",
          "items" : {
            "type" : "string"
          },
          "type" : "array"
        },
        "include_closed" : {
          "description" : "Also list closed (archived) ports, with status 'closed'. Reopen one with port.reopen.",
          "type" : "boolean"
        },
        "space_id" : {
          "description" : "List only this space's ports. Omit to list every space's.",
          "type" : "string"
        }
      },
      "type" : "object"
    },
    "name" : "ports_list"
  },
  {
    "description" : "Make an HTTP request to an external API. Use the 'secret' parameter to inject authentication from the secrets store — you never see the raw credential. Supports GET, POST, PUT, PATCH, DELETE. JSON bodies are auto-serialized. Responses with JSON content-type are auto-parsed.",
    "input_schema" : {
      "properties" : {
        "body" : {
          "description" : "Request body. Objects are JSON-serialized automatically.",
          "type" : "string"
        },
        "headers" : {
          "additionalProperties" : {
            "type" : "string"
          },
          "description" : "Additional HTTP headers as key-value pairs.",
          "type" : "object"
        },
        "method" : {
          "description" : "HTTP method: GET, POST, PUT, PATCH, DELETE. Default: GET.",
          "type" : "string"
        },
        "secret" : {
          "description" : "Named secret from the secrets store. The runtime injects the auth header — you never see the raw key.",
          "type" : "string"
        },
        "timeout" : {
          "description" : "Timeout in milliseconds. Default: 30000, max: 120000.",
          "type" : "integer"
        },
        "url" : {
          "description" : "Full URL to call (https recommended)",
          "type" : "string"
        }
      },
      "required" : [
        "url"
      ],
      "type" : "object"
    },
    "name" : "rest_call"
  },
  {
    "description" : "Execute AppleScript code and return the result. Use this to control other applications on macOS.",
    "input_schema" : {
      "properties" : {
        "source" : {
          "description" : "AppleScript source code",
          "type" : "string"
        },
        "timeout" : {
          "description" : "Timeout in seconds (default: 30, max: 120)",
          "type" : "integer"
        }
      },
      "required" : [
        "source"
      ],
      "type" : "object"
    },
    "name" : "run_applescript"
  },
  {
    "description" : "Execute JavaScript for Automation (JXA) code and return the result. Use this to control other applications on macOS.",
    "input_schema" : {
      "properties" : {
        "source" : {
          "description" : "JXA source code",
          "type" : "string"
        },
        "timeout" : {
          "description" : "Timeout in seconds (default: 30, max: 120)",
          "type" : "integer"
        }
      },
      "required" : [
        "source"
      ],
      "type" : "object"
    },
    "name" : "run_jxa"
  },
  {
    "description" : "Capture a screenshot of the screen. Returns a base64 PNG image.",
    "input_schema" : {
      "properties" : {
        "scale" : {
          "description" : "Image scale factor 0.1-2.0 (default 1.0)",
          "type" : "number"
        }
      },
      "type" : "object"
    },
    "name" : "screen_capture"
  },
  {
    "description" : "Get display information: size, position, and visible area (excluding dock\/menubar) for all connected displays. No screen recording permission required. Use this to calculate port positions before calling port_move.",
    "input_schema" : {
      "properties" : {

      },
      "required" : [

      ],
      "type" : "object"
    },
    "name" : "screen_info"
  },
  {
    "description" : "Record an app surface for a fixed number of seconds and auto-stop (the convenience form). Pass options.seconds. Returns {path, width, height, seconds, fps, bytes}.",
    "input_schema" : {
      "properties" : {
        "options" : {
          "description" : "Recording options: target ({window:\"self\"} | {window:<osId>} | {port:<udid>} | {ports:[<udid>...]} | {region:{x,y,w,h}} | {display:<id>}). window\/port\/ports are occlusion-proof (only that surface); region\/display capture the raw display (may catch other apps). Also aspect (e.g. \"16:9\"), fit (cover|exact; contain is not yet supported), width, height, scale, fps, padding, cursor (bool; only on a display or region target — a window\/port capture cannot include the cursor), audio (none|system|mic|both), format (mov|mp4), path, and for the convenience form seconds.",
          "type" : "object"
        }
      },
      "type" : "object"
    },
    "name" : "screen_record"
  },
  {
    "description" : "Start recording an app surface (window:self \/ a port \/ multiple ports) to a video file with optional system or mic audio. Returns {recordingId, width, height, target}. Stop with screen.record.stop.",
    "input_schema" : {
      "properties" : {
        "options" : {
          "description" : "Recording options: target ({window:\"self\"} | {window:<osId>} | {port:<udid>} | {ports:[<udid>...]} | {region:{x,y,w,h}} | {display:<id>}). window\/port\/ports are occlusion-proof (only that surface); region\/display capture the raw display (may catch other apps). Also aspect (e.g. \"16:9\"), fit (cover|exact; contain is not yet supported), width, height, scale, fps, padding, cursor (bool; only on a display or region target — a window\/port capture cannot include the cursor), audio (none|system|mic|both), format (mov|mp4), path, and for the convenience form seconds.",
          "type" : "object"
        }
      },
      "type" : "object"
    },
    "name" : "screen_record_start"
  },
  {
    "description" : "Report whether a recording (or any recording) is active and its elapsed seconds.",
    "input_schema" : {
      "properties" : {
        "recordingId" : {
          "description" : "Optional recording id; omit for the overall status",
          "type" : "string"
        }
      },
      "type" : "object"
    },
    "name" : "screen_record_status"
  },
  {
    "description" : "Stop a recording started with screen.record.start. Returns {path, width, height, seconds, fps, bytes}.",
    "input_schema" : {
      "properties" : {
        "recordingId" : {
          "description" : "The id returned by screen.record.start",
          "type" : "string"
        }
      },
      "required" : [
        "recordingId"
      ],
      "type" : "object"
    },
    "name" : "screen_record_stop"
  },
  {
    "description" : "List all visible windows with their titles, apps, and positions",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "screen_windows"
  },
  {
    "description" : "Get a space's metadata and member list: { id, name, type, memberCount, members: [{ id, name, type, owner, qualifiedName }] }. Pass space_id to inspect a specific space (e.g. your own PORT42_SPACE_ID); omit it for the currently selected space.",
    "input_schema" : {
      "properties" : {
        "space_id" : {
          "description" : "Optional space id to inspect. Defaults to the currently selected space.",
          "type" : "string"
        }
      },
      "type" : "object"
    },
    "name" : "space_current"
  },
  {
    "description" : "List all spaces the user belongs to",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "space_list"
  },
  {
    "description" : "Delete a value from persistent storage",
    "input_schema" : {
      "properties" : {
        "key" : {
          "description" : "The storage key to delete",
          "type" : "string"
        }
      },
      "required" : [
        "key"
      ],
      "type" : "object"
    },
    "name" : "storage_delete"
  },
  {
    "description" : "Get a value from persistent key-value storage",
    "input_schema" : {
      "properties" : {
        "key" : {
          "description" : "The storage key",
          "type" : "string"
        }
      },
      "required" : [
        "key"
      ],
      "type" : "object"
    },
    "name" : "storage_get"
  },
  {
    "description" : "List all keys in persistent storage",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "storage_list"
  },
  {
    "description" : "Store a value in persistent key-value storage",
    "input_schema" : {
      "properties" : {
        "key" : {
          "description" : "The storage key",
          "type" : "string"
        },
        "value" : {
          "description" : "The value to store",
          "type" : "string"
        }
      },
      "required" : [
        "key",
        "value"
      ],
      "type" : "object"
    },
    "name" : "storage_set"
  },
  {
    "description" : "Execute a shell command and return the output. Runs in \/bin\/zsh.",
    "input_schema" : {
      "properties" : {
        "command" : {
          "description" : "The shell command to execute",
          "type" : "string"
        },
        "cwd" : {
          "description" : "Working directory (default: home)",
          "type" : "string"
        },
        "timeout" : {
          "description" : "Timeout in seconds (default: 30, max: 120)",
          "type" : "integer"
        }
      },
      "required" : [
        "command"
      ],
      "type" : "object"
    },
    "name" : "terminal_exec"
  },
  {
    "description" : "Get the current user's identity (id and display name)",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "user_get"
  },
  {
    "description" : "Who you are to Port42: your name, your space and who is in it (the companions you can @mention), and, for a companion running in a Port42 terminal, that terminal's port id and chat. Call it first.",
    "input_schema" : {
      "properties" : {

      },
      "type" : "object"
    },
    "name" : "whoami"
  }
]